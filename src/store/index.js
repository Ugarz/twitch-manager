/* eslint-disable no-shadow */

import Vue from 'vue';
import Vuex from 'vuex';

// import * as helperReports from '../helpers/reports';

Vue.use(Vuex);

const state = {};

const mutations = {};

const actions = {};

const getters = {};

export default new Vuex.Store({
  state,
  mutations,
  actions,
  getters,
});
